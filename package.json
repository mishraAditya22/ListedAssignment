{
  "name": "listed",
  "version": "1.0.0",
  "description": "The app should check for new emails in a given Gmail ID.   The app should identify and isolate the email threads in which no prior email has been sent by you. This means that the app should only reply to first time email threads sent by others to your mailbox.",
  "main": "index.js",
  "scripts": {
    "start": "cd Server && nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Aditya Mishra",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.4.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "ejs": "^3.1.9",
    "express": "^4.18.2",
    "mongoose": "^7.2.1",
    "nodemailer": "^6.9.2",
    "nodemon": "^2.0.22"
  }
}
